# Workspace rules
# Assign specific apps to workspaces, or set workspace counts.

# Default workspace count
workspace = 1, persistent:true
workspace = 2, persistent:true
workspace = 3, persistent:true
workspace = 4, persistent:true
workspace = 5, persistent:true
workspace = 6, persistent:true

# App-to-workspace assignments (uncomment to enable)
# windowrule = workspace 1, class:^(zen)$
# windowrule = workspace 2, class:^(com\.mitchellh\.ghostty)$
# windowrule = workspace 3, class:^(thunar)$
# windowrule = workspace 5, class:^(spotify-launcher)$
